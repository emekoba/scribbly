import 'package:flutter/material.dart';

class CardParagraph extends StatelessWidget {
  final Map contributor;
  final String blockType;
  final String value;

  const CardParagraph({Key key, this.contributor, this.blockType, this.value})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
